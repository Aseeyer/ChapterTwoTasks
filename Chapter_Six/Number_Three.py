"""
The code goes through each day (k) and its list of temperatures (v) in the dictionary.
For each day, it calculates the average temperature by adding all the temperatures with sum(v),
dividing by how many there are with len(v),
then prints the day and its average formatted to two decimal places.
"""