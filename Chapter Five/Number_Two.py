"""
def mystery(sequence):
    return sequence == sorted(sequence)

The function checks whether the sequence is already sorted in ascending order.
It compares the original sequence with a sorted version of it.
If they are the same, it returns True. Otherwise, it returns False.

"""